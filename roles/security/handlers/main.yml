---
# ============================================
# Role: security — Handlers
# Triggered by 'notify' in tasks
# ============================================

- name: restart sshd
  service:
    name: ssh
    state: restarted
  when: env != 'docker'

- name: restart fail2ban
  service:
    name: fail2ban
    state: restarted
