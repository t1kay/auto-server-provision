---
# ============================================
# Role: common — Handlers
# Triggered by 'notify' in tasks
# ============================================

- name: validate nginx config
  command: nginx -t
  changed_when: false

- name: restart nginx
  service:
    name: nginx
    state: restarted

- name: reload nginx
  service:
    name: nginx
    state: reloaded
